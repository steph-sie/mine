# CCTV Frontend

Minimal Next.js + Tailwind frontend that captures webcam frames and POSTs them to the FastAPI backend at `http://localhost:8000/detect`.

Quick start:

```bash
cd frontend
npm install
npm run dev
```

Open http://localhost:3000
